import { Relationships } from '@amplifi-workspace/home'

export default function RelationshipsLayout({
  children,
}: {
  children: JSX.Element
}) {
  return <Relationships>{children}</Relationships>
}
